repos:
- id: aigubeev/devops-netology
  apply_requirements: [approved]
  workflow: custom
  allowed_overrides: [workflow]
  
workflows:
  custom:
    plan:
      steps:
      - plan:
          extra_args: ["-lock", "false"]
